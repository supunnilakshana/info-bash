import 'package:flutter/material.dart';

class MustUpdateScreen extends StatelessWidget {
  const MustUpdateScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
